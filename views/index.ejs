<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GitHub Deployer - Login</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>Welcome to GitHub Deployer</h1>
        <% if (isAuthenticated) { %>
            <p>Welcome, <%= user ? user.login : 'User' %>!</p>
            <a href="/repos" class="btn">View Repositories</a>
            <a href="/auth/logout" class="btn">Logout</a>
        <% } else { %>
            <p>Connect your GitHub account to deploy your projects.</p>
            <a href="/auth/github" class="btn">Login with GitHub</a>
        <% } %>
    </div>
</body>
</html>
